% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/createsims.R
\name{simcreate_t}
\alias{simcreate_t}
\title{Generate T-Test Simulations}
\usage{
simcreate_t(
  N,
  n.sims = 1000,
  mu = 0,
  data_type = c("summed_bits", "continuous", "integer", "unknown"),
  n_bits = NULL,
  p = 0.5,
  int_min = NULL,
  int_max = NULL,
  method = c("pseudo", "files", "quantis"),
  filespath = NULL,
  parallel = TRUE,
  nstart = 5,
  alternative = c("two.sided", "less", "greater"),
  prior.loc = 0,
  prior.r = 0.353
)
}
\arguments{
\item{N}{Sample size (number of observations after summing/grouping)}

\item{n.sims}{Number of simulations (default: 1000)}

\item{mu}{Null hypothesis mean (default: 0)}

\item{data_type}{Type of data: "summed_bits", "continuous", "integer", or "unknown"}

\item{n_bits}{Number of bits to sum per observation (for summed_bits)}

\item{p}{Probability for each bit (default: 0.5)}

\item{int_min}{Minimum integer value (for integer data_type)}

\item{int_max}{Maximum integer value (for integer data_type)}

\item{method}{Random generation method: "pseudo", "files", or "quantis"}

\item{filespath}{Path to quantum random data file (for method="files")}

\item{parallel}{Use parallel processing (default: TRUE)}

\item{nstart}{Minimum observations before calculating first BF (default: 5)}

\item{alternative}{Alternative hypothesis: "two.sided", "less", or "greater"}

\item{prior.loc}{Prior location parameter (default: 0)}

\item{prior.r}{Prior scale parameter (default: 0.353)}
}
\value{
Dataframe with columns: simid, index, raw, rw, density.rw, bf, density.bf
}
\description{
Generates Monte Carlo simulations for t-tests with support for different data types:
summed bits, continuous normal data, or random integers.
}
\examples{
# Generate t-test simulations with summed bits
sims <- simcreate_t(N = 100, mu = 10, data_type = "summed_bits",
                   n_bits = 20, p = 0.5, n.sims = 100)

# Generate t-test simulations with continuous data
sims <- simcreate_t(N = 100, mu = 0, data_type = "continuous", n.sims = 100)
}
